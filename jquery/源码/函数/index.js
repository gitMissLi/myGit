(function () {

    // 定义一些变量和函数 jQuery = function() {}

    // （146， 226） 给jQ对象增加一些方法或属性
    // jQuery.fn = jQuery.prototype =

    // （227， 295）jQuery 的继承方法
    // jQuery.extend = jQuery.fn.extend

    // （296， 472） 扩展工具方法 $.trim()...
    // jQuery.extend()

    // （499， 2752） 复杂选择器的实现 $('ul li + p div')
    sizzle:

    // （3225，3418） 回调对象： 对函数的统一管理
    Callbacks

    // （3460， 3980）延迟对象： 对异步的统一管理 setTimeout
    Deferred

    // （） 功能检测 不同浏览器版本
    Support

    // （3984，4245）数据缓存
    data()

    // （4329，4395）队列管理
    queue()

    // （4396，4890）对元素属性的操作
    attr() prop() val() addClass() ...

    // （4891，5589） 事件操作的相关方法
    on() trigger()

    // 提交的数据和ajax
    ajax

    // 运动的方法：feedIn。。。
    animate

    // 位置和尺寸的方法
    offset()

    jQ支撑模块化

    // DOM操作 添加 删除 获取 包装 DOM筛选
    before() append()

    // 样式操作
    css()

    // 注册到window
    // window.jQuery = window.$ = jQuery
})()
